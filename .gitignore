.DS_Store
node_modules/
.vscode/ltex*
.cache/

# Astro build output
dist/
.astro/

# OpenSpec: keep only the merged domain specs (openspec/specs/) and config.yaml.
# In-flight/archived changes are planning scaffolding, not committed.
openspec/changes/

# OpenCode/OpenSpec generated tool integration (regenerable via `openspec update`).
.opencode/
