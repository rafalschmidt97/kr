---
import SchedulePage from "../components/SchedulePage.astro";
import { schedules } from "../data/schedules";

const schedule = schedules.find((s) => s.slug === "harmonogram-mlodzi")!;
---

<SchedulePage schedule={schedule} />
